<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <title>Пошук потягів</title>
    <link rel="stylesheet" href="/styles.css">

</head>
<body>
    <div class="container">
        <h1>Результати пошуку</h1>

        <nav class="navbar">
            <a href="/">Головна</a>
            <a href="/admin/search">Пошук</a>
            <a href="/admin">Адмін-панель</a>
        </nav>

        <form action="/api/trains/filter" method="GET">
            <div class="mb-3">
                <label>Звідки:</label>
                <input type="text" name="from" value="<%= from || '' %>" required>
            </div>

            <div class="mb-3">
                <label>Куди:</label>
                <input type="text" name="to" value="<%= to || '' %>" required>
            </div>

            <button type="submit">Пошук</button>
        </form>

        <% if (results.length > 0) { %>
            <ul class="train-list">
                <% results.forEach(train => { %>
                    <li><%= train.name %> з <%= train.from %> до <%= train.to %> о <%= train.departure %></li>
                <% }) %>
            </ul>
        <% } else { %>
            <p class="mt-3">Нічого не знайдено</p>
        <% } %>

        <a href="/admin">
            <button class="admin-btn">Admin Panel</button>
        </a>
    </div>
</body>
</html>
