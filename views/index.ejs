<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <title>Розклад потягів</title>
    <!-- <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"> -->
    <link rel="stylesheet" href="/styles.css"> 

</head>
<body>
    <h1>Розклад потягів</h1>
    <ul>
        <% trains.forEach(train => { %>
            <li><%= train.name %> з <%= train.from %> до <%= train.to %> о <%= train.departure %></li>
        <% }) %>
    </ul>
    <a href="/trains/search">Пошук потяга</a>
</body>
</html>
